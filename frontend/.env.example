# Copy to `.env` and adjust. Create React App only exposes variables prefixed with REACT_APP_.
# Base URL for the Express API (no trailing slash — paths in code start with `/`).
REACT_APP_API_URL=http://localhost:5001
