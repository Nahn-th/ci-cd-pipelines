services:
  backend:
    build: ./backend
    container_name: elearning-be
    ports:
      - "5001:5001"
    env_file:
      - ./backend/.env
    volumes:
      - ./backend:/app
      - /app/node_modules
      - ./backend/data:/app/data
    restart: always
